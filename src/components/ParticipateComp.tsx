
const ParticipateComp = ({heading, para}) => {
    return (
        <div>
            <h1 className="text-lg text-purple-400">{heading}</h1>
            <p className="text-sm">{para}</p>
        </div>
    )
}

export default ParticipateComp